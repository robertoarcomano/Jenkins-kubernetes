apiVersion: v1
kind: Pod
metadata:
  namespace: devops
spec:
  containers:
    - name: kubectl
      image: ubuntu
      tty: true
      volumeMounts:
        - name: kubedir
          mountPath: /root/.kube/config
  volumes:
    - name: kubedir
      hostPath:
        path: /etc/kubernetes/admin.conf